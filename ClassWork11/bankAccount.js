function updateBalance(object, amount) {
    object.balance += amount;
 }
function checkBalance(object) { 
    return balance;
}

function withdraw(object) {
    if amount <= object.balnce{
        object.balance -= amount;
    }
}